[IDENTITY] Codex session 2026-02-24:\nUser asked to reduce overengineering in lua/highlights.lua and prefer direct Neovim API calls over extra Lua orchestration.\n\nRationale and decision process:\n- Removed the TODO marker subsystem (Treesitter query parsing, extmark scanning, refresh scheduling, and related autocmds) because user explicitly chose to drop marker coloring in favor of simplification.\n- Collapsed highlight application to a straightforward API-first flow using direct nvim_set_hl calls for LSP and diagnostics groups.\n- Retained only one small helper for markdown underline suppression; this preserves behavior that cannot be represented by a simple highlight link without reintroducing broader complexity.\n- Kept ColorScheme reapplication in a single augroup callback to preserve runtime resilience after theme changes.\n\nAlternatives considered:\n- Keeping marker coloring with syntax/matchadd fallback: rejected because user selected full removal.\n- Rewriting highlights via vim.cmd('highlight ...'): rejected to stay consistent with requested API-first style.\n\nEvidence:\n- Headless require validation succeeded with local package.path override and require('highlights').\n- Full startup validation succeeded with nvim --headless +qa.\n- hlexists(CustomDiagText)=1 and hlexists(CustomDiagLine)=1 after load, while hlexists(TodoCommentMarkerPending)=0.
