[IDENTITY] Codex session 2026-02-23:\nUser asked to resolve all inline TODOs in lua/highlights.lua. This commit turns those TODOs into implemented behavior instead of aspirational comments.\n\nRationale and trade-offs:\n- Implemented TODO marker highlighting with Treesitter comment-node traversal plus plain-string matching (string.find with plain=true) to satisfy the no-regex preference and keep matching deterministic.\n- Added marker registration as data (marker text + color), then derived highlight groups from the data to keep the flow explicit and easy to extend.\n- Added fallback CustomDiagText/CustomDiagLine highlight setup because custom_diagnostics.lua depends on those groups and not every colorscheme defines them. Used default=true to avoid stomping theme-authored groups.\n- Kept inactive semantic-token clearing for C/C++ and rewrote comments to encode the clangd rationale directly; no safe single LSP switch exists in this config that preserves desired semantic behavior while removing inactive-region dimming.\n- Replaced markdown underline stripping with reusable helper functions rather than one-off inline logic.\n\nAlternatives considered:\n- regex matchadd/hi patterns for TODO markers: rejected per no-regex requirement and lower semantic precision.\n- Treesitter query predicates with #match?: rejected because they still depend on regex patterns for marker detection.\n\nEvidence:\n- headless require check passed with package.path pointed at repo lua/: nvim --headless -u NONE '+lua package.path=...' '+lua require("highlights")' '+qa'.\n- confirmed no remaining '-- TODO' comments in lua/highlights.lua.
