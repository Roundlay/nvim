return {
    "williamboman/mason.nvim",
    cmd = "Mason",
    config = function()
        require("mason").setup({
            ensure_installed = { "all" },
            -- TODO: Get you config from vim-plug.
            -- FROM: https://github.com/LazyVim/LazyVim/search?q=mason
            -- local registry = require("mason-registry")
            -- for _, tool in ipairs(ensure_installed) do
            --     local p = registry.get_package(tool)
            --     if no p:is_installed(tool) then
            --         p:install()
            --     end
            -- end 
        end,
        })
    end,
}
